/**
 * @file Text
 * @author musicode
 */

text-reset()

    button&
        background-color transparent
        outline 0
        border 0
        padding 0


text-size(size)

    ui-set('font-size', size['font'])


ui-text()

    text-reset()

    ui()

    ui-size-variant(
        each: text-size
    )


ui-texts()

    ui()

    ui-collection(
        selector-item: $selector-text,
        each: @(size) {
            > {$selector-text} {
                text-size(size)
            }
        }
    )
