/**
 * @file 徽章
 * @author zhujl
 */

badge() {

    display: inline-block;
    text-align: center;

    ui-size('badge');
}

badge-default() {
    ui-type($type-hash['badge-default']);
}

badge-primary() {
    ui-type($type-hash['badge-primary']);
}

badge-info() {
    ui-type($type-hash['badge-info']);
}

badge-success() {
    ui-type($type-hash['badge-success']);
}

badge-error() {
    ui-type($type-hash['badge-error']);
}
