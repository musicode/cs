/**
 * @file 标签
 * @author zhujl
 */

label() {

    display: inline-block;

    ui-size('label');
}

label-default() {
    ui-type($type-hash['label-default']);
}

label-primary() {
    ui-type($type-hash['label-primary']);
}

label-info() {
    ui-type($type-hash['label-info']);
}

label-success() {
    ui-type($type-hash['label-success']);
}

label-error() {
    ui-type($type-hash['label-error']);
}
