/**
 * @file button
 * @author musicode
 */

@import "../mixin/button.styl"

+prefix-classes($ui-prefix)

    .button

        button()

        ui-brand-variant(
            has-border: true,
            has-color: true,
            has-bg: true,
            has-hover: true,
            has-active: true,
            has-disabled: true
        )

        ui-size-variant(
            has-border: true,
            has-font: true,
            has-line-height: true,
            has-padding: true
        )

        ui-clickable(
            has-disabled: true
        )

        &.link
            ui-link(
                color: $link-color,
                has-hover: true
            )

        &.round
            ui-size-variant(
                has-round: true
            )

        &.radius
            ui-size-variant(
                has-radius: true
            )

        &.fluid
            ui-fluid()

    .buttons
        buttons()

        > .button

            &.round,
            &.radius
                border-radius 0

            margin-left -1px

            &:first-child
                margin-left 0

            ui-size-variant(
                has-left-round: true,
                child-selector: '&.round:first-child'
            )

            ui-size-variant(
                has-right-round: true,
                child-selector: '&.round:last-child'
            )

            ui-size-variant(
                has-left-radius: true,
                child-selector: '&.radius:first-child'
            )

            ui-size-variant(
                has-right-radius: true,
                child-selector: '&.radius:last-child'
            )



