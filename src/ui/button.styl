/**
 * @file 按钮
 * @author zhujl
 */

@import "../variable/button.styl";
@import "../mixin/button.styl";

button {
    outline: none;
    // safari 需要写这句
    margin: 0;
}

.btn-default,
.btn-primary,
.btn-info,
.btn-success,
.btn-error,
.btn-link {
    button();
}

.btn-default {
    button-default();
}

.btn-primary {
    button-primary();
}

.btn-info {
    button-info();
}

.btn-success {
    button-success();
}

.btn-error {
    button-error();
}

.btn-link {
    button-link();
}

.btn-block {
    btn-block();
}