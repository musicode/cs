/**
 * @file 按钮
 * @author zhujl
 */

.btn-default,
.btn-primary,
.btn-info,
.btn-success,
.btn-error,
.btn-link {
    button();
    button-size();
}

.btn-default {
    button-default();
}

.btn-primary {
    button-primary();
}

.btn-info {
    button-info();
}

.btn-success {
    button-success();
}

.btn-error {
    button-error();
}

.btn-link {
    button-link();
}
