/**
 * @file Pagination
 * @author musicode
 */

+prefix-classes($ui-prefix)

    {$selector-pagination}
        ui-pagination()

        &{$selector-tight}
            ui-pagination-tight()

        > {$selector-pagination-item}
            ui-pagination-item()


