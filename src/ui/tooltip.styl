/**
 * @file 工具提示
 * @author musicode
 */

@import "../variable/tooltip.styl"
@import "../mixin/tooltip.styl"

+prefix-classes($ui-prefix)

    .tooltip

        tooltip()

        &.reversed
            tooltip-reversed()

        &.top
            tooltip-top()

        &.right
            tooltip-right()

        &.bottom
            tooltip-bottom()

        &.left
            tooltip-left()

