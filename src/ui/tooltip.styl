/**
 * @file 工具提示
 * @author zhujl
 */

@import "../variable/tooltip.styl";
@import "../mixin/tooltip.styl";

.tooltip {
    tooltip();
}
.tooltip-default {
    tooltip-default();
}
.tooltip-inverted {
    tooltip-inverted();
}

.tooltip-top {
    tooltip-top();
}
.tooltip-right {
    tooltip-right();
}
.tooltip-bottom {
    tooltip-bottom();
}
.tooltip-left {
    tooltip-left();
}
