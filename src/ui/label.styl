/**
 * @file Label
 * @author musicode
 */

@import '../variable/label.styl'
@import '../mixin/label.styl'

+prefix-classes($ui-prefix)

    {$selector-label}
        ui-label()

    {$selector-labels}
        ui-labels()

        &{$selector-tight}
            ui-labels-tight()

