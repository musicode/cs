/**
 * @file 标签
 * @author zhujl
 */

@import "../variable/label.styl";
@import "../mixin/label.styl";

.label-default,
.label-primary,
.label-info,
.label-success,
.label-error {
    label();
}

.label-default {
    label-default();
}

.label-primary {
    label-primary();
}

.label-info {
    label-info();
}

.label-success {
    label-success();
}

.label-error {
    label-error();
}