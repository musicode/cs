/**
 * @file 标签
 * @author zhujl
 */

.label-huge {
    label-huge();
}
.label-large {
    label-large();
}
.label {
    label-base();
}
.label-small {
    label-small();
}
.label-tiny {
    label-tiny();
}

.label-default {
    label-default();
}

.label-primary {
    label-primary();
}

.label-info {
    label-info();
}

.label-success {
    label-success();
}

.label-error {
    label-error();
}