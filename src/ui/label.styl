/**
 * @file 标签
 * @author zhujl
 */

.label-default,
.label-primary,
.label-info,
.label-success,
.label-error {
    label();
    label-size();
}

.label-default {
    label-default();
}

.label-primary {
    label-primary();
}

.label-info {
    label-info();
}

.label-success {
    label-success();
}

.label-error {
    label-error();
}