/**
 * @file 列表
 * @author musicode
 */

+prefix-classes($ui-prefix)

    {$selector-list}
        ui-list()

        > {$selector-list-item}
            ui-list-item()

        > a{$selector-list-item}
            text-decoration none
            ui-set('color', $brand-text)

