/**
 * @file 进度条
 * @author musicode
 */

@import "../mixin/progress.styl"

.{$ui-prefix}progress

    progress()

    ui-size-variant(has-font: true, has-line-height: true)

    &.round
        ui-size-variant(has-round: true)

    &.radius
        ui-size-variant(has-radius: true)


.{$ui-prefix}meter

    meter()

    ui-brand-variant(has-bg: true)