/**
 * @file 消息
 * @author zhujl
 */

.message-default,
.message-primary,
.message-info,
.message-success,
.message-error {
    ui-size('message', false, true);
}

.message-default {
    message-default();
}
.message-primary {
    message-primary();
}
.message-info {
    message-info();
}
.message-success {
    message-success();
}
.message-error {
    message-error();
}
