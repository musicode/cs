/**
 * @file 图片
 * @author musicode
 */

@import "../variable/image.styl";
@import "../mixin/image.styl";

+prefix-classes($ui-prefix)

    .image

        image()

        ui-size-variant(
            size-hash: $image-size-hash,
            has-width: true,
            has-height: true
        )

        {$selector-round}
            ui-size-variant(
                size-hash: $image-size-hash,
                has-round: true
            )

        {$selector-radius}
            ui-size-variant(
                size-hash: $image-size-hash,
                has-radius: true
            )

