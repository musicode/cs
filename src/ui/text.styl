/**
 * @file 文本
 * @author musicode
 */

@import "../mixin/text.styl"

.{$ui-prefix}text

    text()

    for type in $brand-hash
        &.{type}
            ui-brand-variant(brand: $brand-hash[type])


