/**
 * @file 排版
 * @author musicode
 */

@import "../variable/typography.styl"

+prefix-classes($ui-prefix)

    html
        box-sizing border-box

    *,
    *:before,
    *:after
        box-sizing inherit

    body
        background-color $bg-color
        color $text-color
        font-family $font-family-title;
        font-size $size-base['font-size'];
        line-height $size-base['line-height']

    .link
        ui-link(
            color: $link-color,
            has-hover: true
        )
