/**
 * @file 排版
 * @author zhujl
 */

body {
    background-color: $bg-color;
    color: $text-color;
    font: $font-size-base/$line-height-base $font-family-title;
}

a {
    color: $link-color;
    text-decoration: none;

    &:hover {
        color: $link-color-hover;
    }
}

small {
    font-size: $font-size-tiny;
}