/**
 * @file 表单元素
 * @author zhujl
 */

input[type="search"] {
    appearance: textfield;
}

.form-label,
.form-static,
.form-radio,
.form-checkbox,
.form-inline,
.form-block {
    ui-size('form');
}

.form-action {
    form-action();
}

/**
.form-group {
    form-group();

    .success,
    .error {
        display: none;
        font-size: $font-size-tiny;
    }
    .success {
        color: $brand-success;
    }
    .error {
        color: $brand-error;
    }

    &.has-success {
        .success {
            display: inline-block;
        }
    }
    &.has-error {
        .error {
            display: inline-block;
        }
    }
}
*/
.form-label {
    form-label();
}

.form-controls {
    form-controls();
}

.form-radio,
.form-checkbox {
    form-box();
}


.form-text {

    form-text();
    ui-size('form-text', true, true);

}

.form-inline {
    form-inline();
}
.form-block {
    form-block();
}
