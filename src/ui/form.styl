/**
 * @file 表单元素
 * @author zhujl
 */

input[type="search"] {
    appearance: textfield;
}

.form-label,
.form-static,
.form-radio,
.form-checkbox,
.form-inline,
.form-block {
    ui-size('form');
}

.form-group {
    form-group();
}

.form-action {
    form-action();
}

.form-label {
    form-label();
}

.form-controls {
    form-controls();
}

.form-radio,
.form-checkbox {
    form-box();
}


.form-text {

    form-text();
    ui-size('form-text', true, true);

}

.form-inline {
    form-inline();
}
.form-block {
    form-block();
}
