/**
 * @file 缩略图
 * @author zhujl
 */

@import "../variable/thumbnail.styl";
@import "../mixin/thumbnail.styl";

.thumbnail,
.thumbnail-round,
.thumbnail-circle {

    thumbnail();

    .title {
        thumbnail-title();
    }
}

.thumbnail {
    thumbnail-rect();
}

.thumbnail-round {
    thumbnail-round();
}

.thumbnail-circle {
    thumbnail-circle();
}