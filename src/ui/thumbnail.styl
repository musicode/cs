/**
 * @file 缩略图
 * @author zhujl
 */

.thumbnail,
.thumbnail-round,
.thumbnail-circle {
    thumbnail();
}

.thumbnail {
    thumbnail-rect();
}

.thumbnail-round {
    thumbnail-round();
}

.thumbnail-circle {
    thumbnail-circle();
}