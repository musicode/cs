/**
 * @file 徽章
 * @author zhujl
 */

.badge-default,
.badge-primary,
.badge-info,
.badge-success,
.badge-error {
    badge();
    badge-size();
}

.badge-default {
    badge-default();
}

.badge-primary {
    badge-primary();
}

.badge-info {
    badge-info();
}

.badge-success {
    badge-success();
}

.badge-error {
    badge-error();
}
