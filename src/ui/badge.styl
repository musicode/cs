/**
 * @file 徽章
 * @author zhujl
 */

@import "../variable/badge.styl";
@import "../mixin/badge.styl";

.badge-default,
.badge-primary,
.badge-info,
.badge-success,
.badge-error {
    badge();
}

.badge-default {
    badge-default();
}

.badge-primary {
    badge-primary();
}

.badge-info {
    badge-info();
}

.badge-success {
    badge-success();
}

.badge-error {
    badge-error();
}
