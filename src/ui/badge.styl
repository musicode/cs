/**
 * @file Badge
 * @author musicode
 */

+prefix-classes($ui-prefix)

    {$selector-badge}
        ui-badge()

    {$selector-badges}
        ui-badges()

        &{$selector-tight}
            ui-collection-tight(
                selector-item: $selector-badge
            )
