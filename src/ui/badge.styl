/**
 * @file 徽章
 * @author musicode
 */

@import "../mixin/badge.styl";

.{$ui-prefix}badge

    badge();

    ui-brand-variant(has-bg: true)
    ui-size-variant(has-font: true, has-line-height: true, has-padding: true, has-round: true)
