/**
 * @file 徽章
 * @author zhujl
 */

.badge-huge {
    badge-huge();
}
.badge-large {
    badge-large();
}
.badge {
    badge-base();
}
.badge-small {
    badge-small();
}
.badge-tiny {
    badge-tiny();
}

.badge-default {
    badge-default();
}

.badge-primary {
    badge-primary();
}

.badge-info {
    badge-info();
}

.badge-warning {
    badge-warning();
}

.badge-success {
    badge-success();
}

.badge-error {
    badge-error();
}
