/**
 * @file 工具提示
 * @author zhujl
 */

$tooltip-bg = #555;
$tooltip-border = none;
$tooltip-color = #FFF;
$tooltip-shadow = 1px 1px 1px rgba(0,0,0,0.2);

$size-hash.tooltip = {
    'font-size-base': 12px,
    'padding-vertical': 5px,
    'padding-horizontal': 12px,
    'border-radius': 4px
};
