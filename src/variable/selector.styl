/**
 * @file 选择器
 * @author musicode
 */

$selector-hover = '&.hover, &:hover'
$selector-active = '&.active, &:active'
$selector-focus = '&.focus, &:focus'
$selector-disabled = '&.disabled, &[disabled]'
$selector-checked = '&.checked, &[checked]'
$selector-first-child = '&.first-child, &:first-child'
$selector-last-child = '&.last-child, &:last-child'

$selector-link = '&.link'
$selector-round = '&.round'
$selector-radius = '&.radius'
$selector-fluid = '&.fluid'
$selector-justified = '&.justified'
