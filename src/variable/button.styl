/**
 * @file Button
 * @author musicode
 */

$selector-button = '.button'
$selector-buttons = '.buttons'

$button-icon-gap = 6px

$button-box-shadow-active = inset 0px 1px 4px rgba(0,0,0,0.15)
$button-border-darken = 10%
