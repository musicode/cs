/**
 * $file Form
 * $author musicode
 */

$selector-form = '.form'
$selector-form-input = '.input'
$selector-form-placeholder = '.placeholder'
$selector-form-group = '.group'
$selector-form-action = '.action'
$selector-form-controls = '.controls'
$selector-icon-input = '.icon-input'
$selector-input-icon = '.input-icon'
$selector-form-radio = '.radio'
$selector-form-checkbox = '.checkbox'

$form-placeholder-color = #DDD
$form-input-border = #DDD
$form-input-bg = #FFF
$form-input-border-hover = #CCC
$form-input-border-focus = $brand-primary['base']

$form-input-icon-width = 30px

$form-group-gap-inline = 8px
$form-group-gap-block = 10px

$form-control-gap-inline = 8px
$form-control-gap-block = 6px

$form-box-text-gap = 6px
$form-box-gap-horizontal = 10px
$form-box-gap-vertical = 8px

$form-action-gap-vertical = 20px
$form-action-button-gap-horizontal = 10px

$form-input-size-hash = {
    tiny: {
        height: 28px
    },
    small: {
        height: 32px
    },
    '': {
        height: 36px
    },
    large: {
        height: 46px
    },
    huge: {
        height: 58px
    }
}
