/**
 * $file Form
 * $author musicode
 */

$selector-form = '.form'
$selector-form-input = '.input'
$selector-form-inputs = '.inputs'
$selector-form-input-group = '.input-group'
$selector-form-menu = '.menu'
$selector-form-placeholder = '.placeholder'
$selector-form-group = '.group'
$selector-form-footer = '.footer'
$selector-form-controls = '.controls'
$selector-form-radio = '.radio'
$selector-form-radios = '.radios'
$selector-form-checkbox = '.checkbox'
$selector-form-checkboxes = '.checkboxes'


$form-input-bg = #FFF
$form-input-border-hover-lighten = 30%

$form-input-bg-disabled = #EEE
$form-input-border-disabled = #CCC
$form-input-color-disabled = #999

$form-placeholder-color = #CCC

$form-input-icon-width = 30px

$form-menu-bg = $brand-bg
$form-menu-gap = 0
$form-menu-box-shadow = $layer-box-shadow

$form-group-gap-inline = 8px
$form-group-gap-block = 15px

$form-control-gap-inline = 8px
$form-control-gap-block = 8px

$form-box-text-gap = 6px

$form-footer-gap-vertical = 30px

$form-box-size-hash = {
    '': $size-base,
    tiny: $size-tiny,
    small: $size-small,
    large: $size-large,
    huge: $size-huge
}

$form-input-height-hash = {
    '': 36px,
    tiny: 20px,
    small: 26px,
    large: 44px,
    huge: 58px
}

$form-input-bg-lighten = 97%

$form-input-brand-hash = {
    primary: {
        bg: lighten($brand-primary, $form-input-bg-lighten),
        border: $brand-primary
    },
    secondary: {
        bg: #FFF,
        border: $brand-border
    },
    success: {
        bg: lighten($brand-success, $form-input-bg-lighten),
        border: $brand-success
    },
    error: {
        bg: lighten($brand-error, $form-input-bg-lighten),
        border: $brand-error
    },
    info: {
        bg: lighten($brand-info, $form-input-bg-lighten),
        border: $brand-info
    },
    warning: {
        bg: lighten($brand-warning, $form-input-bg-lighten),
        border: $brand-warning
    },
    danger: {
        bg: lighten($brand-danger, $form-input-bg-lighten),
        border: $brand-danger
    },
    muted: {
        bg: lighten($brand-muted, $form-input-bg-lighten),
        border: $brand-muted
    }
}

$form-input-size-hash = {
    '': $size-base,
    tiny: $size-tiny,
    small: $size-small,
    large: $size-large,
    huge: $size-huge
}

$form-input-radius-hash = {
    '': $radius-base,
    tiny: $radius-tiny,
    small: $radius-small,
    large: $radius-large,
    huge: $radius-huge
}

