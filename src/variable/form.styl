/**
 * $file 表单元素
 * $author zhujl
 */

$size-hash['form'] = {

    'base': {
        'padding-horizontal': 0px
    },

    'tiny': {
        'padding-horizontal': 0px
    },

    'small': {
        'padding-horizontal': 0px
    },

    'large': {
        'padding-horizontal': 0px
    },

    'huge': {
        'padding-horizontal': 0px
    }
};

$size-hash['form-text'] = {

    'base': {
        'border-top-width': 1px,
        'border-right-width': 1px,
        'border-bottom-width': 1px,
        'border-left-width': 1px,
        'border-radius': $border-radius-base
    },

    'tiny': {
        'border-top-width': 1px,
        'border-right-width': 1px,
        'border-bottom-width': 1px,
        'border-left-width': 1px,
        'border-radius': $border-radius-base
    },

    'small': {
        'border-top-width': 1px,
        'border-right-width': 1px,
        'border-bottom-width': 1px,
        'border-left-width': 1px,
        'border-radius': $border-radius-base
    },

    'large': {
        'border-top-width': 1px,
        'border-right-width': 1px,
        'border-bottom-width': 1px,
        'border-left-width': 1px,
        'border-radius': $border-radius-base
    },

    'huge': {
        'border-top-width': 1px,
        'border-right-width': 1px,
        'border-bottom-width': 1px,
        'border-left-width': 1px,
        'border-radius': $border-radius-base
    }
};


$size-hash['form-hint'] = {
    'base': {
        'font-size': $font-size-tiny,
        'padding-vertical': 0,
        'padding-horizontal': 0
    }
};

$type-hash['form-hint'] = {
    'default': {
        'color': $brand-info
    }
};

/**
 * ================================================
 * form-group
 * ================================================
 */
$form-group-gap = 10px;
$form-group-border = none;

/**
 * ================================================
 * form-label
 * ================================================
 */
$form-label-font-weight = normal;
$form-label-gap = 5px;

/**
 * ================================================
 * form-required
 * ================================================
 */
$form-required-color = $brand-error;
$form-required-font-size = $font-size-tiny;
$form-required-gap = 5px;


/**
 * ================================================
 * form-box
 * ================================================
 */
$form-box-inner-gap = 5px;
$form-box-gap = 10px;


/**
 * ================================================
 * form-action
 * ================================================
 */
$form-action-gap = 20px;


/**
 * ================================================
 * form-inline
 * ================================================
 */
$form-inline-gap = 5px;


/**
 * ================================================
 * form-block
 * ================================================
 */
$form-block-gap = 8px;


/**
 * ================================================
 * form-text
 * ================================================
 */
$form-text-color-placeholder = #CCC;

$type-hash['form-text'] = {

    'default': {
        'bg': #FFF,
        'border': #CCC,
        'color': $text-color
    },

    'hover': {
        'bg': #FFF,
        'border': $brand-primary,
        'color': $text-color
    },

    'focus': {
        'bg': #FFF,
        'border': $brand-primary,
        'color': $text-color
    },

    'disabled': {
        'bg': $gray-lighter,
        'border': $gray-light,
        'color': $gray-light
    }
};

$type-hash['input-group-addon'] = {

    'default': {
        'bg': #EFEFEF,
        'border': #CCC,
        'color': $gray-darker
    }
};
