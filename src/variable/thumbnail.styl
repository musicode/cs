/**
 * @file 缩略图
 * @author zhujl
 */

$thumbnail-border = #DDD;
$thumbnail-padding-vertical = 1px;
$thumbnail-padding-horizontal = 1px;
$thumbnail-font-size = 12px;
$thumbnail-line-height = 0px;

$thumbnail-round = 4px;
$thumbnail-round-inner = 2px;

$thumbnail-circle = 1000px;
$thumbnail-circle-inner = 1000px;


$thumbnail-width-base = 80px;
$thumbnail-height-base = 80px;

$thumbnail-width-tiny = 50px;
$thumbnail-height-tiny = 50px;

$thumbnail-width-small = 60px;
$thumbnail-height-small = 60px;

$thumbnail-width-large = 120px;
$thumbnail-height-large = 120px;

$thumbnail-width-huge = 150px;
$thumbnail-height-huge = 150px;


$size-hash['thumbnail-rect'] = {

    'width-base': $thumbnail-width-base,
    'height-base': $thumbnail-height-base,
    'font-size-base': $thumbnail-font-size,
    'line-height-base': $thumbnail-line-height,
    'padding-vertical-base': $thumbnail-padding-vertical,
    'padding-horizontal-base': $thumbnail-padding-horizontal,
    'border-radius-base': 0px,

    'width-tiny': $thumbnail-width-tiny,
    'height-tiny': $thumbnail-height-tiny,
    'font-size-tiny': $thumbnail-font-size,
    'line-height-tiny': $thumbnail-line-height,
    'padding-vertical-tiny': $thumbnail-padding-vertical,
    'padding-horizontal-tiny': $thumbnail-padding-horizontal,
    'border-radius-tiny': 0px,

    'width-small': $thumbnail-width-small,
    'height-small': $thumbnail-height-small,
    'font-size-small': $thumbnail-font-size,
    'line-height-small': $thumbnail-line-height,
    'padding-vertical-small': $thumbnail-padding-vertical,
    'padding-horizontal-small': $thumbnail-padding-horizontal,
    'border-radius-small': 0px,

    'width-large': $thumbnail-width-large,
    'height-large': $thumbnail-height-large,
    'font-size-large': $thumbnail-font-size,
    'line-height-large': $thumbnail-line-height,
    'padding-vertical-large': $thumbnail-padding-vertical,
    'padding-horizontal-large': $thumbnail-padding-horizontal,
    'border-radius-large': 0px,

    'width-huge': $thumbnail-width-huge,
    'height-huge': $thumbnail-height-huge,
    'font-size-huge': $thumbnail-font-size,
    'line-height-huge': $thumbnail-line-height,
    'padding-vertical-huge': $thumbnail-padding-vertical,
    'padding-horizontal-huge': $thumbnail-padding-horizontal,
    'border-radius-huge': 0px

};

$size-hash['thumbnail-round'] = {

    'width-base': $thumbnail-width-base,
    'height-base': $thumbnail-height-base,
    'font-size-base': $thumbnail-font-size,
    'line-height-base': $thumbnail-line-height,
    'padding-vertical-base': $thumbnail-padding-vertical,
    'padding-horizontal-base': $thumbnail-padding-horizontal,
    'border-radius-base': 0px,

    'width-tiny': $thumbnail-width-tiny,
    'height-tiny': $thumbnail-height-tiny,
    'font-size-tiny': $thumbnail-font-size,
    'line-height-tiny': $thumbnail-line-height,
    'padding-vertical-tiny': $thumbnail-padding-vertical,
    'padding-horizontal-tiny': $thumbnail-padding-horizontal,
    'border-radius-tiny': 0px,

    'width-small': $thumbnail-width-small,
    'height-small': $thumbnail-height-small,
    'font-size-small': $thumbnail-font-size,
    'line-height-small': $thumbnail-line-height,
    'padding-vertical-small': $thumbnail-padding-vertical,
    'padding-horizontal-small': $thumbnail-padding-horizontal,
    'border-radius-small': 0px,

    'width-large': $thumbnail-width-large,
    'height-large': $thumbnail-height-large,
    'font-size-large': $thumbnail-font-size,
    'line-height-large': $thumbnail-line-height,
    'padding-vertical-large': $thumbnail-padding-vertical,
    'padding-horizontal-large': $thumbnail-padding-horizontal,
    'border-radius-large': 0px,

    'width-huge': $thumbnail-width-huge,
    'height-huge': $thumbnail-height-huge,
    'font-size-huge': $thumbnail-font-size,
    'line-height-huge': $thumbnail-line-height,
    'padding-vertical-huge': $thumbnail-padding-vertical,
    'padding-horizontal-huge': $thumbnail-padding-horizontal,
    'border-radius-huge': 0px

};

$size-hash['thumbnail-circle'] = {

    'width-base': $thumbnail-width-base,
    'height-base': $thumbnail-height-base,
    'font-size-base': $thumbnail-font-size,
    'line-height-base': $thumbnail-line-height,
    'padding-vertical-base': $thumbnail-padding-vertical,
    'padding-horizontal-base': $thumbnail-padding-horizontal,
    'border-radius-base': 0px,

    'width-tiny': $thumbnail-width-tiny,
    'height-tiny': $thumbnail-height-tiny,
    'font-size-tiny': $thumbnail-font-size,
    'line-height-tiny': $thumbnail-line-height,
    'padding-vertical-tiny': $thumbnail-padding-vertical,
    'padding-horizontal-tiny': $thumbnail-padding-horizontal,
    'border-radius-tiny': 0px,

    'width-small': $thumbnail-width-small,
    'height-small': $thumbnail-height-small,
    'font-size-small': $thumbnail-font-size,
    'line-height-small': $thumbnail-line-height,
    'padding-vertical-small': $thumbnail-padding-vertical,
    'padding-horizontal-small': $thumbnail-padding-horizontal,
    'border-radius-small': 0px,

    'width-large': $thumbnail-width-large,
    'height-large': $thumbnail-height-large,
    'font-size-large': $thumbnail-font-size,
    'line-height-large': $thumbnail-line-height,
    'padding-vertical-large': $thumbnail-padding-vertical,
    'padding-horizontal-large': $thumbnail-padding-horizontal,
    'border-radius-large': 0px,

    'width-huge': $thumbnail-width-huge,
    'height-huge': $thumbnail-height-huge,
    'font-size-huge': $thumbnail-font-size,
    'line-height-huge': $thumbnail-line-height,
    'padding-vertical-huge': $thumbnail-padding-vertical,
    'padding-horizontal-huge': $thumbnail-padding-horizontal,
    'border-radius-huge': 0px

};


$thumbnail-bg = #FFF;


$thumbnail-shadow = none;

$thumbnail-title-color = $text-color;
$thumbnail-title-gap = 8px;

$thumbnail-mask-hover = rgba(0,0,0,0.3);


