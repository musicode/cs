/**
 * @file Dropdown
 * @author musicode
 */

$selector-dropdowns = '.dropdowns'

$selector-dropdown = '.dropdown'
$selector-dropdown-menu = '.menu'
$selector-dropdown-menu-item = '.item'

$dropdown-icon-gap = 10px
$dropdown-menu-gap = 2px
$dropdown-menu-max-height = 176px

$dropdown-menu-box-shadow = $layer-box-shadow
$dropdown-menu-item-box-shadow-active = inset 0px 1px 2px rgba(0,0,0,0.05)
