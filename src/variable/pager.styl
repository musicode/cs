/**
 * @file 翻页
 * @author zhujl
 */

$pager-item-gap = 0px;

$size-hash['pager-btn'] = {

    'base': {
        'border-top-width': 1px,
        'border-right-width': 1px,
        'border-bottom-width': 1px,
        'border-left-width': 1px,
        'padding-vertical': 6px,
        'padding-horizontal': 13px
    }
};

$size-hash['pager-ellipsis'] = {
    'base': {
        'padding-vertical': 7px,
        'padding-horizontal': 3px
    }
};

$type-hash['pager-btn'] = {

    'default': {
        'bg': #FFF,
        'border': #DDD,
        'color': $gray-dark
    }

    'hover': {
        'bg': #FFF,
        'border': $brand-primary,
        'color': $brand-primary
    },

    'active': {
        'bg': $brand-primary,
        'border': $brand-primary,
        'color': #FFF
    }
};




