/**
 * @file Tabs
 * @author musicode
 */

$selector-tab = '.tab'
$selector-tabs = '.tabs'

$tab-color = #222

$tabs-brand-hash = {
    primary: {
        color: $tab-color,
        color-hover: $brand-primary,
        color-active: $brand-primary,
        color-checked: $brand-primary
    }
}

$tabs-size-hash = {
    large: $size-large,
    huge: $size-huge
}
